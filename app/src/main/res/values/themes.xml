<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ProyectoCancha" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema para la Splash Screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/grey_900</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item> <!-- CAMBIO AQUÍ -->
        <item name="postSplashScreenTheme">@style/Theme.ProyectoCancha</item>
    </style>
</resources>